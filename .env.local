# true = usa mocks hardcoded (no necesitas backend)
# false = llama al backend real via proxy en /api
NEXT_PUBLIC_USE_MOCKS=false

# Solo necesario cuando NEXT_PUBLIC_USE_MOCKS=false
# Next.js proxiea /api/* a esta URL server-side (sin CORS)
BACKEND_URL=http://localhost:8090/api